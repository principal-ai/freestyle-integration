{
  "name": "freestyle-git-import-repro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal SDK-based reproduction of Freestyle Git server-side import failures (git.repos.create({source})).",
  "scripts": {
    "repro": "tsx src/repro.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "freestyle": "^0.1.63"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
